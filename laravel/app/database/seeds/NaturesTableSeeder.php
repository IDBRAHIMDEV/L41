<?php

class NaturesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('natures')->truncate();

		$natures = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('natures')->insert($natures);
	}

}
